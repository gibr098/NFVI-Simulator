# Configuration file of a NFVI-PoP

#DataCenter(number of Servers)
number_of_servers = 5

#Server's resources :
RAM(GB) = 16
CPU(Cores) = 8
CPU(speed_in_GHz) = 2.20
Storage(GB) = 64
Network(interfaces) = 12

#Number of container per sever
containers = 4

#Server's computational capacity needed by the container:
container_cpu(%) = 20

#Container's resources needed:
C-RAM(GB) = 4
C-CPU(Cores) = 2
C-CPU(speed_in_GHz) = 2.20
C-Storage(GB) = 8
C-Network(interfaces) = 3

#Policy(FIFO, EDF, SJF, RR)
policy = FIFO

#Simulation parameters:
time_of_simulation = 100

# Rate of requests arrival
lambda = 0.1

